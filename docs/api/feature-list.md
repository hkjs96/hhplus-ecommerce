# 기능 리스트 (Feature List)

## 프로젝트 범위
**항해플러스 2주차 기본 과제** - 이커머스 핵심 기능 구현

---

## 📦 1. 상품 관리

### 1.1 상품 조회
- **기능**: 상품 목록 조회
- **API**: `GET /api/products`
- **Query Parameters**:
  - `category` (optional): 카테고리 필터
  - `sort` (optional): 정렬 (price/popularity/newest)
- **응답**: 상품 목록 (id, name, price, stock, category)

### 1.2 상품 상세 조회
- **기능**: 특정 상품 상세 정보 조회
- **API**: `GET /api/products/{productId}`
- **응답**: 상품 상세 (id, name, description, price, stock, category)

### 1.3 인기 상품 통계
- **기능**: 최근 3일간 인기 상품 Top 5
- **API**: `GET /api/products/top`
- **구현 방식**: 실시간 MySQL 쿼리
- **Response**:
  ```json
  {
    "period": "3days",
    "products": [
      {
        "rank": 1,
        "productId": "P001",
        "name": "노트북",
        "salesCount": 150,
        "revenue": 133500000
      }
    ]
  }
  ```
- **Fallback**: 조회 실패 시 빈 배열 반환

> **설계 결정**: 초기에는 실시간 쿼리로 단순하게 구현하고, 성능 이슈 발생 시 배치/캐시로 개선 (Week 2 피드백 반영)

---

## 💳 2. 주문/결제 시스템

### 2.1 주문 생성
- **기능**: 새 주문 생성
- **API**: `POST /api/orders`
- **Request**:
  ```json
  {
    "userId": "user123",
    "items": [
      {
        "productId": "P001",
        "quantity": 2
      }
    ],
    "couponId": "COUPON_10"
  }
  ```
- **Response**:
  ```json
  {
    "orderId": "ORDER-20240115-001",
    "userId": "user123",
    "items": [
      {
        "productId": "P001",
        "name": "노트북",
        "quantity": 2,
        "unitPrice": 890000,
        "subtotal": 1780000
      }
    ],
    "subtotalAmount": 1780000,
    "discountAmount": 178000,
    "totalAmount": 1602000,
    "status": "PENDING",
    "createdAt": "2024-01-15T10:30:00Z"
  }
  ```
- **프로세스**:
  1. 재고 확인 (충분한지 검증)
  2. 쿠폰 검증 (있다면)
  3. 주문 금액 계산 (소계, 할인, 최종 금액)
  4. 주문 생성 (상태: PENDING)
- **Error**: P002 (재고 부족), O001 (잘못된 수량), C002/C003/C004 (쿠폰 오류)

### 2.2 결제 처리
- **기능**: 주문에 대한 결제 실행
- **API**: `POST /api/orders/{orderId}/payment`
- **Request**:
  ```json
  {
    "userId": "user123"
  }
  ```
- **Response**:
  ```json
  {
    "orderId": "ORDER-20240115-001",
    "paidAmount": 1602000,
    "remainingBalance": 398000,
    "status": "SUCCESS",
    "dataTransmission": "SUCCESS",
    "paidAt": "2024-01-15T10:35:00Z"
  }
  ```
- **프로세스**:
  1. 주문 조회 (PENDING 상태 확인)
  2. 잔액 확인 (충분한지 검증)
  3. **트랜잭션 시작**
  4. 잔액 차감
  5. 재고 차감 (Pessimistic Lock)
  6. 쿠폰 사용 처리 (있다면)
  7. 주문 상태 변경 (PENDING → PAID)
  8. **트랜잭션 커밋**
  9. 외부 데이터 전송 (비동기)
- **dataTransmission**: SUCCESS / FAILED / PENDING
  - **중요**: 외부 전송 실패(`FAILED`)여도 주문은 정상 완료 처리
- **Error**: PAY001 (잔액 부족), O002 (주문 없음), O003 (주문 상태 오류)

---

## 🎟️ 3. 쿠폰 시스템

### 3.1 쿠폰 발급 (선착순)
- **기능**: 한정 수량 쿠폰 선착순 발급
- **API**: `POST /api/coupons/{couponId}/issue`
- **Request**:
  ```json
  {
    "userId": "user123"
  }
  ```
- **Response**:
  ```json
  {
    "userCouponId": "UC-20240115-001",
    "couponId": "COUPON_10",
    "couponName": "10% 할인쿠폰",
    "discountRate": 10,
    "status": "AVAILABLE",
    "issuedAt": "2024-01-15T10:00:00Z",
    "expiresAt": "2024-12-31T23:59:59Z",
    "remainingQuantity": 95
  }
  ```
- **프로세스**:
  1. **트랜잭션 시작**
  2. 중복 발급 체크 (user_id + coupon_id unique)
  3. 쿠폰 조회 (Optimistic Lock - version 필드)
  4. 수량 체크 (issued_quantity < total_quantity)
  5. 발급 수량 증가 (issued_quantity++)
  6. 사용자 쿠폰 생성 (status: AVAILABLE)
  7. **트랜잭션 커밋**
- **동시성 제어**: Optimistic Lock (version 필드)
- **Error**: C001 (쿠폰 소진), C004 (중복 발급), 버전 충돌 시 재시도

### 3.2 보유 쿠폰 조회
- **기능**: 사용자가 보유한 쿠폰 목록
- **API**: `GET /api/users/{userId}/coupons`
- **Query Parameters**:
  - `status` (optional): 쿠폰 상태 필터 (AVAILABLE/USED/EXPIRED)
- **Response**:
  ```json
  {
    "userId": "user123",
    "coupons": [
      {
        "userCouponId": "UC-20240115-001",
        "couponId": "COUPON_10",
        "couponName": "10% 할인쿠폰",
        "discountRate": 10,
        "status": "AVAILABLE",
        "issuedAt": "2024-01-15T10:00:00Z",
        "expiresAt": "2024-12-31T23:59:59Z"
      }
    ],
    "totalCount": 1
  }
  ```

### 3.3 쿠폰 검증
- **기능**: 주문 시 쿠폰 유효성 검증 (내부 로직)
- **검증 항목**:
  - 쿠폰 존재 여부
  - 쿠폰 소유자 일치
  - 상태 (AVAILABLE)
  - 만료일 확인

---

## 🔗 4. 외부 데이터 플랫폼 연동

### 4.1 주문 데이터 전송
- **기능**: 주문 완료 후 외부 플랫폼에 데이터 전송
- **트리거**: 결제 완료 후
- **실행 방식**: 비동기 (@Async)
- **프로세스**:
  1. 주문 데이터 가공 (내부 형식 → 외부 형식)
  2. 외부 API 호출 (Timeout: 3초)
  3. 성공 → 로그 기록
  4. 실패 → Fallback 실행
- **Fallback**:
  - **설계**: Redis Queue 포함
  - **Week 2 Mock**: In-Memory Queue (ConcurrentLinkedQueue)
  - 주문은 정상 완료 처리 ✅
- **재시도 메커니즘**:
  - 스케줄러가 주기적으로 큐 확인 (1분, 5분, 30분 후)
  - 최대 재시도 횟수 제한

### 4.2 데이터 변환
- **기능**: 내부 Order 모델 → 외부 API 형식 변환
- **변환 내용**:
  - 필드 매핑
  - 날짜 형식 변환
  - 추가 메타데이터 포함

---

## 👤 5. 사용자 관리 (지원 기능)

### 5.1 사용자 조회
- **기능**: 사용자 정보 조회 (주문/결제에 필요)
- **API**: `GET /api/users/{userId}`
- **응답**: 사용자 정보 (id, username, email, balance)

### 5.2 잔액 조회
- **기능**: 사용자 잔액 조회
- **API**: `GET /api/users/{userId}/balance`
- **응답**: 잔액 정보

### 5.3 잔액 충전
- **기능**: 사용자 잔액 충전 (테스트용)
- **API**: `POST /api/users/{userId}/balance/charge`
- **Request Body**:
  - `amount`: 충전 금액
- **응답**: 충전 후 잔액

---

## 📊 총 API 엔드포인트 (기본 과제)

### 상품 (3개)
1. `GET /api/products` - 상품 목록
2. `GET /api/products/{id}` - 상품 상세
3. `GET /api/products/top` - 인기 상품

### 주문/결제 (2개)
4. `POST /api/orders` - 주문 생성
5. `POST /api/orders/{id}/payment` - 결제 처리

### 쿠폰 (2개)
6. `POST /api/coupons/{id}/issue` - 쿠폰 발급
7. `GET /api/users/{id}/coupons` - 보유 쿠폰 조회

### 사용자 (3개)
8. `GET /api/users/{id}` - 사용자 조회
9. `GET /api/users/{id}/balance` - 잔액 조회
10. `POST /api/users/{id}/balance/charge` - 잔액 충전

**총 10개 API 엔드포인트**

---

## 🎯 핵심 비즈니스 로직

### 재고 관리
```
재고 차감 시점: 결제 완료 후
동시성 제어: Pessimistic Lock (FOR UPDATE)
실패 처리: 트랜잭션 롤백으로 자동 복원
검증: stock >= quantity
```

### 쿠폰 관리
```
발급 제어: Optimistic Lock (version 필드)
중복 방지: DB Unique Constraint (user_id, coupon_id)
수량 제어: issued_quantity < total_quantity
```

### 외부 연동
```
실행: 비동기 (@Async)
Timeout: 3초
Fallback: 재시도 큐 저장
핵심: 외부 실패가 주문 완료를 막지 않음
```

---

## 🔄 주요 프로세스 플로우

### 1. 전체 주문 플로우
```
1. 상품 조회 (재고 확인)
2. 주문 생성 (POST /api/orders)
   → 재고 검증
   → 쿠폰 검증 (선택)
   → 주문 금액 계산
   → 주문 생성 (PENDING)
3. 결제 처리 (POST /api/orders/{id}/payment)
   → 잔액 확인
   → 트랜잭션 시작
   → 잔액 차감
   → 재고 차감 (Lock)
   → 쿠폰 사용
   → 주문 상태 변경 (PAID)
   → 트랜잭션 커밋
   → 외부 전송 (비동기)
```

### 2. 쿠폰 발급 플로우
```
1. 쿠폰 발급 요청 (POST /api/coupons/{id}/issue)
2. 트랜잭션 시작
3. 중복 발급 체크
4. 쿠폰 조회 (Optimistic Lock)
5. 수량 체크
6. 발급 수량 증가
7. 사용자 쿠폰 생성
8. 트랜잭션 커밋
9. 실패 시 롤백 or 재시도
```

### 3. 인기 상품 집계 플로우
```
1. 스케줄러 실행 (5분마다)
2. 최근 3일 주문 데이터 조회
3. 상품별 판매량/매출 집계
4. Top 5 선정
5. 캐시 저장 (In-Memory)
6. 실패 시: 이전 캐시 데이터 유지
```

---

## ✅ 구현 체크리스트

### Phase 1: 기본 엔티티 및 Repository
- [ ] Product 엔티티 및 Repository
- [ ] User 엔티티 및 Repository
- [ ] Order 엔티티 및 Repository
- [ ] OrderItem 엔티티
- [ ] Coupon 엔티티 및 Repository
- [ ] UserCoupon 엔티티 및 Repository

### Phase 2: 상품 기능
- [ ] 상품 목록 조회 API
- [ ] 상품 상세 조회 API
- [ ] 인기 상품 배치 집계 (Scheduler)
- [ ] 인기 상품 조회 API (with Fallback)

### Phase 3: 쿠폰 기능
- [ ] 쿠폰 발급 API (Optimistic Lock)
- [ ] 보유 쿠폰 조회 API
- [ ] 쿠폰 검증 로직

### Phase 4: 주문/결제 기능
- [ ] 주문 생성 API (재고 검증)
- [ ] 결제 처리 API (트랜잭션)
- [ ] 재고 차감 (Pessimistic Lock)
- [ ] 잔액 차감
- [ ] 쿠폰 사용 처리

### Phase 5: 외부 연동
- [ ] 외부 API 클라이언트 (Mock)
- [ ] 비동기 전송 (@Async)
- [ ] Timeout 설정 (3초)
- [ ] Fallback 구현 (재시도 큐)
- [ ] 재시도 스케줄러

### Phase 6: 사용자 기능 (지원)
- [ ] 사용자 조회 API
- [ ] 잔액 조회 API
- [ ] 잔액 충전 API

### Phase 7: 테스트
- [ ] 단위 테스트 (도메인 로직)
- [ ] 통합 테스트 (API)
- [ ] 동시성 테스트 (재고/쿠폰)
- [ ] 외부 API 장애 시나리오 테스트

---

## 🚫 제외 사항 (기본 과제 범위 밖)

### 확장 기능 (선택)
- 장바구니 (심화 과제)
- 배송 관리 (심화 과제)
- 주문 내역 조회 (심화 과제)
- 토스페이 연동 (심화 과제)

### Week 2 (현재): 설계 + Mock 📐
- ✅ **설계**: Redis/Kafka 포함한 완전한 시스템 설계
- ✅ **Mock 서버**: In-Memory 컬렉션 사용
- ✅ **산출물**: API 명세, ERD, 시퀀스 다이어그램


### 간소화된 부분
- ❌ 회원가입/로그인 (userId만 사용)
- ❌ 관리자 기능 (최소한만 구현)

---

## 📝 다음 단계

1. ✅ 요구사항 분석 완료
2. ✅ 기능 리스트 정리 완료
3. ⏭️ ERD 작성 (엔티티 관계 정의)
4. ⏭️ 시퀀스 다이어그램 작성 (핵심 플로우)
5. ⏭️ API 명세서 상세화
6. ⏭️ 구현 시작
